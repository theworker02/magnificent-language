# Predictive Engine

Run:

```bash
mgl predict examples/main.mgl
mgl predict --game examples/unity/player.mgl
```

The predictive engine simulates branches, loops, basic data flow, and game lifecycle methods before runtime.
